# Handoff — session of 2026-08-07 → 08-11 (read after 00-HANDOFF)

The previous handoff (00) described the tool refactor. This one hands off a
much larger arc: five library generations measured, the gate/replay/practice
infrastructure built and battle-tested, and — most importantly — a LIVE
DESIGN DISCUSSION with the owner that the next session must continue, not
restart. Written at the owner's request as the session neared its limit.

---

## The discussion thread to CONTINUE (owner's priority)

**Topic: what "debugging" must mean in this harness.** The full arc:

1. Owner asked for ASPIRE-style debugging. I built `rpent/practice` — a
   between-episode loop (attempt → observe → update playbook → attempt
   again), escalating to `--experiment` episodes when knowledge stalls.
2. Owner pushed back twice, correctly: (a) "where is the step from failure
   to TRYING things?" — my loop is reflect-and-retry (Reflexion-shaped),
   trying only in the escape hatch; (b) "ASPIRE debugs WITHIN the task" —
   and on forced re-read of the paper, the owner is right in the way that
   matters:
3. **ASPIRE's debugger is a RESIDENT session**: one Claude Code session
   (Opus 4.6, 1M ctx) per task; write program → execute in engine → read
   curated per-primitive traces → patch → re-execute, ALL INSIDE ONE
   CONTINUOUS CONTEXT that remembers every prior attempt. BEHAVIOR-1K mode
   goes further: "incremental block execution, generating each next code
   block from the current multimodal trace" — code written DURING
   execution. My practice loop summons an amnesiac updater per round —
   that difference (continuous memory vs fresh context) is the soul lost.
4. Owner's closing insight: **our LangGraph turn-loop is ALREADY a live
   session** — in-episode it debugs continuously. What blocks residency
   is three self-imposed walls:
   - episode boundary = session boundary (env done → session killed);
   - the single-attempt regime (right for exams, wrong for practice);
   - context budget (flash vs their 1M — solvable by folding each failed
     attempt's raw tool messages into a digest, ASPIRE-style curation;
     the long-parked CompactionMiddleware's first legitimate use).

> **2026-08-11 (later同日): BUILT.** The four pieces below are implemented and
> regression-tested (`tests/harness/test_resident.py` drives a scripted
> two-attempt session end-to-end). Entry point: `rpent ... --resident
> --sandbox practice` (deepagents planner, seed >= 51 enforced). One decision
> was added in review with the owner: **folding is only safe with a
> retrieval channel** — each digest carries its citation key
> `[attempt N seq M]`, and a new `view_attempt_call(attempt, seq)` tool
> resolves it into the full archived record (the existing `read_text_file`
> reads head-first and cannot address a call without re-inflating the
> context). Decision record: 02-decisions.md, "Resident debug sessions".

**AGREED NEXT BUILD — the resident debug session (practice seeds only):**

1. `reset_episode` tool, capability-gated: EXISTS only under the practice
   sandbox (read_image-variant pattern; exam runs never see the tool —
   mechanism, not prohibition). `env.reset` RPC already exists.
2. Attempt rotation: on reset, archive the episode's artifacts
   (states.json collides on step numbering otherwise) into `attempt_NN/`;
   v1 shortcut approved: simple rename, replay may see only the last
   attempt, refine later.
3. Per-attempt context folding middleware (fold prior failed attempt's
   tool messages into its run_digest; reasoning stays; raw stays on disk).
4. Practice-session brief: goal = debug until the task solves reliably,
   then write the recipe into the playbook; the gate audits as usual.
5. Alternative discussed (not chosen yet): use the repo's `claude_code`
   planner as the literal ASPIRE actor (1M ctx resident programmer with
   the rpent CLI as its execution engine). Costlier, most faithful.

**Owner's success criterion**: crack ONE never-solved task with this
method — t6 is the target (0/11 on seed 0 all-time). Quality over speed;
GPU cost of single-task iteration is acceptable.

**Closing Q&A of the session (owner confirmed the design):** How does
ASPIRE know which "failure-adjacent keyframes + return values" to show?
Answer: the selection is STRUCTURAL, not learned — the engine snapshots
every primitive call's entry/exit unconditionally; a failing call is
located by its own return status (code is its own stack trace); the
resident agent then pulls evidence by call index. We already own the
same layer: tool_calls.jsonl (per-call, with step_idx before/after) +
step-indexed artifacts + replay's per-tool renderers + citation tokens.
The resident session's folding digest = run_digest, drill-down = read
by seq. Our one real delta: ASPIRE's actor is multimodal (sees frames);
flash is text-only, so "looking at a keyframe" degrades to querying
that step's geometry — read_image is ready if the coder model ever goes
multimodal. RAG/external memory stores: deferred by owner; note the
disk record already IS the external memory (citation tokens are the
retrieval keys), so future RAG = an index over what exists, and the
one-line library index is its v0.

**Control arm already running**: the amnesiac `rpent/practice` loop was
left running on t6 (check `logs/practice_t6.out` + 
`logs/practice_libero_object_swap_t6/history.json` on the box). Whatever
it achieves is the baseline the resident session must beat.

## RESULT (2026-08-11 afternoon): t6 IS CRACKED — seed 0 went 0/11 → 3/3

The full arc ran the same day the machinery was built:

- **Control arm (amnesiac loop)**: 5 rounds, 115 min — 0 solves, 2
  playbook entries, ended "no new knowledge". Its last two rounds ran
  with the SAME playbook the resident session started from, which is
  what makes the comparison knowledge-fair.
- **Resident session** (t6 s51, 454 s agent time, 74 calls, in 2.4M):
  attempt 1 solved after ONE in-place recovery — the within-session
  contrast "pi0 from HOME closes fingers to 0.002 (air grasp, success
  flag true) vs pre-positioned pick closes to 0.039 (pinching the
  0.040 slab)" is knowledge the amnesiac loop structurally cannot
  form; reset; attempt 2 reproduced the solve in 11 calls.
  `resident_notes.md` holds the recipe with per-step `[attempt N seq M]`
  citations. Local copy: `replays/resident_t6_s51/`.
- **Gate**: two revise proposals mined from the run; numbers all traced;
  both HELD by the contract check (revise requires human confirmation)
  — the owner reviewed and approved both. Note surfaced: the strongest
  refuting evidence (the 0.002 air-grasp) lives in `attempt_01/`, which
  gate citation resolution cannot reach (v1 rotation shortcut) — see
  04-open-issues.
- **Exam, seed 0** (playbook-armed practice profile, single attempt, no
  resident tools on the surface): **3/3 solved** — terminated at env
  steps 8 / 15 / 7 (run dirs `20260811-13:48:21`, `-14:02:01`,
  `-14:18:59` …`_t6_s0`; local copies `replays/exam[1-3]_t6_s0/`). All
  four playbook entries consumed (seq 5-8 in exam 1) and followed.
  Historical baseline: 0/11 all-time, including "recipes read AND
  followed" failures — the delta is today's two revisions.

The Friday-doc sentence this earns, calibrated: the harness turned an
opaque policy failure into a measured discriminator with a reproducible
fix, every claim citable to call-level evidence — and the first
never-solved cell fell to it, 3/3 on repeat. (n=3 on one cell; no
curve claims.)

## Results this session (all fingerprinted, all on the box)

- **Generations, seed 0, single runs**: gen0 4/10 → gen1 5/10 → gen2 7/10
  → gen3 6/10 → gen4 5/10. The "growth curve" reading DIED under repeats:
- **Repeats (n=3/arm, same code head)**: none = 4,6,6 (mean 5.33);
  memory@gen4 = 5,8,5 (mean 6.0). Real effect ≈ +0.7 with ±1.5 per-sweep
  noise, concentrated in the middle band (t0/t1/t4/t8/t9: 9/15 vs 7/15).
  Easy cells (t2/t3/t7) don't need the library; dead cells (t5/t6) it
  can't save (perception-bound). REPORT MEAN±SPREAD, NEVER SINGLE RUNS.
- **Library**: 21 entries (gen1-5: gen5 was a pure tightening cycle — 5
  revises, 0 adds) + the first task playbook (memory/tasks/
  libero_object_swap/t6/, 2 entries). Ledger: reading turned selective in
  gen2 (consultation tax died, t0/t2 recovered) but selectivity ORPHANED
  the anti-spiral entry (zombie while t6 spiraled) → one-line index is a
  data-backed need, not taste.
- **t6 saga** (the case study): experiments cracked localisation
  (low-slab elimination: enumerate namables, low-z sweep, raised-slab
  discriminator at table_z+0.008..0.035 — transferred across seeds) and
  the grasp prompt (shape-descriptive "flat butter slab" beat name-only;
  SOLVED s52 mid-experiment). Exam on seed 0: recipes read AND followed,
  still failed — root cause now isolated to pi0's flat-slab grasp
  reliability (policy layer, not knowledge). Failure attribution moved
  two walls in one day.
- **Five HARNESS_REVIEWs** (logs/gate_gen1..5/, local copies in
  replays/gate_reviews/): summary quality visibly improves per cycle —
  human repairs went 1 YAML → 4 bad paths + YAML → 1 YAML → 1 scope → 0.

## Infrastructure built this session

Chronological: tool_docs + leak lint → sandbox profiles (always-on,
fail-closed) → prompt leveling (capability-based) → evidence replay
(2D timeline + 3D point-cloud, citation anchors seq-N) → promotion gate
(observe/synthesize/review/apply/ledger; 4 verbs; 5 mechanical checks) →
CaP arm (Algorithm-1 faithful, PARKED: plumbing proven,
flash-as-coder insufficient on t3; removed from this branch, still on
`refactor/langchain-native-tools`) → experiment episodes (--experiment,
practice-seed-only) → task-playbook tier (scope=task) + practice profile →
amnesiac practice loop (`rpent/practice`).

Bugs found by our own tooling and fixed: DeepSeek-400 orphan tool_calls
(ToolCallIntegrityMiddleware: pre-flight repair + crime-scene dump),
essay-drift silent death (one nudge), post-termination crash (episode-over
guard in ctx.advance), empty-playbook advertising, "(none)" placeholder
question, program runs leaving zero evidence (cap api instrument),
sandbox fingerprint missing write_denied.

## Queued work (approved, not started)

1. **Resident debug session** — the agreed build, top priority.
2. **Perception overhaul package**: LocateAnything wiring
   (enumerate-with-category → per-candidate colour/size stats tool → the
   planner picks; PR #2 merged, server unwired; 8GB VRAM won't fit beside
   the sim on the 24GB card — run on demand), collapse-rejection hook,
   Thea-style post-action verification hooks (graduate the top-support
   verify entries into mechanisms), one-line library index.
3. t5 experiment records still unmined (observe works without notes).
4. **Friday 2026-08-14: mentor Draft PR + doc.** Materials ready:
   repeats numbers + stratified table, t6 case study, review evolution
   line, ASPIRE positioning (docs/harness/06), Thea positioning (owner
   pasted the full paper in-conversation: embodied harness on real
   robots — Scene Graph as Context, Evaluation as Exit Codes with forced
   post-hooks, 93.3% evaluator accuracy, retry math; their update_memory
   writes freely — our niche is measured, gated, clean-room growth).

   **Positioning paragraph agreed with the owner (08-11): "the VLA
   harness is grown, not adopted."** Answers the inevitable reviewer
   question "why not deepagents / Claude Code's harness". Desktop-agent
   harnesses encode four assumptions embodiment breaks:
   (a) actions are reversible and cheap → they optimize throughput
   (parallelism, speculative work, subagents); embodiment demands
   evidence-before-commitment (advancing/read-only split, staleness,
   post-action verification);
   (b) the world IS text → their virtual filesystem; ours: heavy data on
   disk, citations in context — in embodiment text is only a projection
   through measurement;
   (c) a cheap oracle exists ("run the tests") → embodiment must BUILD
   its judge (Thea's Evaluation-as-Exit-Codes is that construction;
   even the env's own flags lie, cf. pi0_pick.success);
   (d) tasks decompose into independent contexts (subagent fan-out) →
   an episode is one continuous world and one body; only read-only
   perception over dumped artifacts may fan out.
   LangGraph is the shared chassis; the harness is grown twice over —
   hand-grown from (embodied constraint × project rule: mechanism over
   discipline, attribution, measure-don't-recall), and SELF-growing
   through the gate with the growth itself measured (generation curve
   with repeats, ledger, review-quality line). That double meaning is
   the positioning: Thea is the same species but writes memory freely
   and measures no generations; ASPIRE has the system but not the
   measurement.

## Process discipline (learned the hard way)

- **Bundle box commits back IMMEDIATELY** — two divergence incidents, one
  briefly lost gen3/4 memory commits (recovered via cherry-pick).
  Sync route: git bundle over ssh, both directions; github unreachable
  from the box.
- `source env.sh` BEFORE `cd` (env.sh cds to the OLD repo).
- `pkill -f` self-match: use `[.]` patterns (`rpent[.]practice`), or you
  kill your own ssh session (happened; caused a false "solved" reading).
- Launch long jobs with `(nohup ... < /dev/null &)` in a subshell.
- Owner communication: plain Chinese, no jargon-piling; their metaphors
  (小本子/翻库税/补一脚) are canon. Owner reads replays and gate reviews
  directly — keep local copies in replays/ fresh.
